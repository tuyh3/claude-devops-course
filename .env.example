# ========================================
# 环境变量配置示例文件
# ========================================
#
# 使用说明：
# 1. 复制此文件为 .env
#    cp .env.example .env
# 2. 根据实际环境修改配置值
# 3. .env 文件不应提交到 Git（已在 .gitignore 中排除）
#
# ========================================

# ==================== 应用配置 ====================
APP_PORT=8080
SPRING_PROFILES_ACTIVE=dev
LOG_LEVEL=DEBUG

# ==================== Docker 配置 ====================
DOCKER_REGISTRY=registry.example.com
IMAGE_TAG=latest
BUILD_NUMBER=local
GIT_COMMIT=unknown
BUILD_DATE=2025-01-01T00:00:00Z

# ==================== Oracle 数据库配置 ====================
DB_URL=jdbc:oracle:thin:@//192.168.1.66:1521/ORCL
DB_USERNAME=system
DB_PASSWORD=your_oracle_password

# ==================== PostgreSQL 数据库配置（开发/测试环境）====================
POSTGRES_PORT=5432
POSTGRES_DB=devopsdb
POSTGRES_USER=devops
POSTGRES_PASSWORD=postgres123

# ==================== Redis 配置 ====================
REDIS_PORT=6379
REDIS_PASSWORD=redis123

# ==================== Nginx 配置 ====================
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# ==================== ELK Stack 配置（独立部署的日志中间件）====================
# Logstash 地址（应用程序发送日志）
LOGSTASH_HOST=localhost
LOGSTASH_PORT=5000

# Elasticsearch 地址（可选，用于直接查询）
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200

# Kibana 地址（可选，用于访问 Web UI）
KIBANA_HOST=localhost
KIBANA_PORT=5601

# ==================== 监控配置 ====================
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin

# ==================== CI/CD 配置 ====================
# SonarQube
SONAR_HOST_URL=http://sonarqube.example.com
SONAR_TOKEN=your_sonar_token

# 部署服务器
DEV_SERVER=dev-server.example.com
TEST_SERVER=test-server.example.com
PROD_SERVER=prod-server.example.com

# SSH 密钥
SSH_PRIVATE_KEY=your_ssh_private_key_content

# GitLab Webhook
GITLAB_WEBHOOK_SECRET=your_webhook_secret

# 通知 Webhook（钉钉/企业微信/飞书）
WEBHOOK_URL=https://your-webhook-url

# Docker Registry 凭证
CI_REGISTRY_USER=your_registry_username
CI_REGISTRY_PASSWORD=your_registry_password

# ==================== 其他配置 ====================
# 时区
TZ=Asia/Shanghai
